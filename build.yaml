# Cyboard Imprint user-config: board=assimilator-bt + shields=imprint_left/right。
# このファイルが board/shield 一覧の唯一のソース（CI 通常ビルド
# .github/workflows/build.yml と Release ビルド release.yml が共通参照する）。
# 追加・改名時はここを編集するだけで両方追従する。
# west.yml の特殊事情（@main 固定不可・HWv2 依存）は CLAUDE.md を参照。
---
include:
  - board: assimilator-bt
    shield: imprint_left
  - board: assimilator-bt
    shield: imprint_right
  - board: xiao_ble
    shield: imprint_dongle
