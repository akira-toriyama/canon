# Prospector Dongle = t-ogura prospector_scanner shield on xiao_ble/nrf52840/zmk.
# A BLE observer that renders the Imprint Dongle's status advertisement
# (CONFIG_ZMK_STATUS_ADVERTISEMENT on the imprint_dongle build). It never pairs
# or connects, so nothing here touches the split link or the chord contract.
# Mirrors config/prospector_scanner.conf of t-ogura/zmk-config-prospector
# (v2.2.3) except for the hardware section at the bottom.

# ============== Display ==============
CONFIG_ZMK_DISPLAY=y
CONFIG_DISPLAY=y
CONFIG_LVGL=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y
CONFIG_LV_Z_MEM_POOL_SIZE=49152
CONFIG_LV_Z_DOUBLE_VDB=y
CONFIG_LV_Z_VDB_SIZE=30
CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED=y
CONFIG_ZMK_DISPLAY_DEDICATED_THREAD_STACK_SIZE=16384
CONFIG_MAIN_STACK_SIZE=4096
# FPU registers are shared across threads (display + BLE); without this the
# context switch corrupts them.
CONFIG_FPU_SHARING=y
CONFIG_GPIO=y

# ============== LVGL widgets / fonts used by the layouts ==============
CONFIG_LV_USE_LABEL=y
CONFIG_LV_USE_BAR=y
CONFIG_LV_USE_FLEX=y
CONFIG_LV_USE_ARC=y
CONFIG_LV_USE_CANVAS=y
CONFIG_LV_USE_IMAGE=y
CONFIG_LV_USE_SLIDER=y
CONFIG_LV_USE_SWITCH=y
CONFIG_LV_USE_BUTTON=y
CONFIG_LV_FONT_MONTSERRAT_12=y
CONFIG_LV_FONT_MONTSERRAT_16=y
CONFIG_LV_FONT_MONTSERRAT_18=y
CONFIG_LV_FONT_MONTSERRAT_20=y
CONFIG_LV_FONT_MONTSERRAT_28=y
CONFIG_LV_FONT_DEFAULT_MONTSERRAT_20=y
CONFIG_LV_FONT_UNSCII_8=y
CONFIG_LV_FONT_UNSCII_16=y

# ============== Scanner mode ==============
CONFIG_BT=y
CONFIG_BT_OBSERVER=y
CONFIG_PROSPECTOR_MODE_SCANNER=y
# 0=YADS 1=Field 2=Operator 3=Radii (touch is off, so this is the only selector).
CONFIG_PROSPECTOR_DEFAULT_LAYOUT=1

# ============== Backlight ==============
CONFIG_PWM=y
CONFIG_LED=y
CONFIG_LED_PWM=y

# ============== Off ==============
CONFIG_ZMK_SPLIT=n
# A display, not an input device. With the defaults the scanner enumerates on
# the host as an empty "Prospector" HID keyboard plus a CDC ACM console (ZMK's
# default VID/PID, the same pair as the Imprint Dongle). Three switches make
# USB power-only:
# - ZMK_USB=n drops the HID class (the board defconfig turns it on).
# - BOARD_SERIAL_BACKEND_CDC_ACM=n drops the xiao_ble board's CDC ACM console
#   set (Zephyr boards/common/usb: SERIAL / CONSOLE / UART_CONSOLE and the
#   stack itself). With the stack off but that set on, the UART console and log
#   backend fail to link against the missing zephyr,console CDC device.
# - USB_DEVICE_STACK=n is still needed because ZMK's app/Kconfig defaults it on
#   for every nRF USBD board on its own.
# HWINFO used to come free with the USB stack (device serial number); the
# module's fault_recovery.c calls hwinfo_get_reset_cause() without selecting it.
# ZMK_BLE cannot follow: scanner mode never calls bt_enable() itself and relies
# on ZMK's ble.c for it, so the connectable "Prospector" advertisement stays
# until the custom shield (t-5gxp) owns the BT bring-up. --logging builds
# re-select the stack, CDC ACM and the console, as intended.
CONFIG_ZMK_USB=n
CONFIG_BOARD_SERIAL_BACKEND_CDC_ACM=n
CONFIG_USB_DEVICE_STACK=n
CONFIG_HWINFO=y
CONFIG_ZMK_STUDIO=n
CONFIG_PM_DEVICE=n
CONFIG_I2C=y
# The Cyboard module's imprint shield defaults ZMK_RGB_UNDERGLOW=y module-wide
# (outside its SHIELD_ guard), so every build in this manifest inherits it and
# rgb_underglow.c then #errors without a zmk,underglow chosen node.
CONFIG_ZMK_RGB_UNDERGLOW=n

# ============== Hardware: beekeeb pre-soldered unit ==============
# No APDS9960 on this unit, so brightness is fixed. The touch panel is present
# but unwired: keep TOUCH_ENABLED=n (non-touch build).
CONFIG_PROSPECTOR_TOUCH_ENABLED=n
CONFIG_PROSPECTOR_USE_AMBIENT_LIGHT_SENSOR=n
CONFIG_PROSPECTOR_FIXED_BRIGHTNESS=80
